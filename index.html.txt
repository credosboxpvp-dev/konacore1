<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ekibimiz</title>
    <style>
        body {
            margin: 0; padding: 0;
            background: #111827; /* Koyu arka plan */
            background: linear-gradient(180deg, #111827 0%, #1e293b 100%);
            font-family: 'Inter', sans-serif;
            color: white; min-height: 100vh;
            display: flex; flex-direction: column; align-items: center;
        }

        /* Kar Tanesi Efekti */
        .snowflake {
            color: #fff; position: fixed; top: -10%; z-index: 999;
            user-select: none; animation: fall linear infinite;
        }
        @keyframes fall { 0% { top: -10%; } 100% { top: 100%; } }

        /* Başlık */
        .header {
            margin-top: 50px; text-align: center;
            display: flex; flex-direction: column; align-items: center; gap: 10px;
        }
        .header h1 { font-size: 3.5rem; margin: 0; display: flex; align-items: center; gap: 15px; }
        .snow-icon { color: #7dd3fc; font-size: 3rem; }

        /* Kart Konteynırı */
        .container {
            display: flex; justify-content: center; flex-wrap: wrap;
            padding: 50px; gap: 30px;
        }

        /* Ekran görüntüsündeki Cam Kart Tasarımı */
        .card {
            background: rgba(30, 41, 59, 0.5);
            backdrop-filter: blur(10px);
            border-radius: 40px;
            padding: 40px;
            width: 320px;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.05);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.4);
        }

        /* Profil Fotoğrafları */
        .avatar {
            width: 160px; height: 160px;
            border-radius: 50%;
            border: 4px solid #0f172a;
            margin-bottom: 25px;
            object-fit: cover;
            box-shadow: 0 0 15px rgba(0,0,0,0.5);
        }

        /* İsimler ve Roller (Yeşil Yazı) */
        .card h2 { color: #4ade80; margin: 10px 0; font-size: 1.8rem; font-weight: bold; }
        .card p { color: #94a3b8; font-size: 1.1rem; margin: 0; }

        /* Alt Parlama Çizgisi */
        .glow-line {
            width: 60%; height: 2px;
            background: linear-gradient(90deg, transparent, #4ade80, transparent);
            margin: 20px auto 0;
            opacity: 0.5;
        }
    </style>
</head>
<body>

    <div class="header">
        <h1><span class="snow-icon">❄️</span> Ekibimiz</h1>
    </div>

    <div class="container">
        <div class="card">
            <img src="https://raw.githubusercontent.com/cdexecctru/fileupld/main/fbarubl5/31baba.jpg" alt="jujustukaisen31" class="avatar">
            <h2>jujustukaisen31</h2>
            <p>Ekip Üyesi / Geliştiriciki</p>
            <div class="glow-line"></div>
        </div>

        <div class="card">
            <img src="https://raw.githubusercontent.com/cdexecctru/fileupld/main/e0pq30z8/TTNI_NETWORK.png" alt="curse_prime64" class="avatar">
            <h2>curse_prime64</h2>
            <p>Ekip Üyesi / Yönetici</p>
            <div class="glow-line"></div>
        </div>
    </div>

    <script>
        function createSnowflake() {
            const snowflake = document.createElement('div');
            snowflake.classList.add('snowflake');
            snowflake.innerHTML = '❄';
            snowflake.style.left = Math.random() * 100 + 'vw';
            snowflake.style.animationDuration = Math.random() * 3 + 4 + 's';
            snowflake.style.opacity = Math.random();
            document.body.appendChild(snowflake);
            setTimeout(() => snowflake.remove(), 7000);
        }
        setInterval(createSnowflake, 150);
    </script>
</body>
</html>
